# 概览

## 集成到你的DevOps中

`FuzzX` 会与你的源代码管理和持续集成\(CI\)的工具相结合\(例如`GitHub`\)，以此来确保我们所`Fuzzing`的代码总是最近一次提交的。

`FuzzX` 会自动检测您的漏洞是否已被修复。

## 简洁、方便的测试框架

`FuzzX` 会自动生成测试用例，这将为开发者节省大量的考虑如何写出覆盖边缘的测试用例的时间。

## 自动化提示

正常情况下，`FuzzX`会默默运行在后台，一旦发现了新的缺陷，`FuzzX`会自动发出警告。

